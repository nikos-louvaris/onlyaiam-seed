# secrets — never commit
*.env
secrets.env
.env.master
*.token
*.key

# python
__pycache__/
*.pyc

# os
.DS_Store

# runtime memory artifacts — γεμίζουν ζώντας, δεν ταξιδεύουν κενά/test (tracked README/template μένουν)
memory/edges/edges.json
.pytest_cache/

# private genesis/audit history — owner refs, never ship
_genesis/

# build-time test fixtures — not part of the seed
_fixtures/
