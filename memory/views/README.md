# views/ — compiled views (χρονοσημασμένα, με παλμό)

Κενό στον σπόρο. Ο Renewal Engine (φάση ④) γράφει εδώ compiled views: σύντομες
συνθέσεις ανά subject, με `compiled_at` + `supporting_slugs`. Δες `_TEMPLATE.md`.

**Όριο:** view = pointer-με-freshness, ΠΟΤΕ truth. Η σύνθεση γεννιέται query-time
(recall_law). Stale view → ξαναχτίζεται· το παλιό → `_archive/` (ποτέ διαγραφή).
